---
interface Props {
  title?: string;
  body?: string;
}

const { title, body } = Astro.props;
---

<>
  {title && <h2 class="text-xl m-0 capitalize">{title}</h2>}
  {body && <p class="m-0 text-base">{body}</p>}
  <slot />
</>
